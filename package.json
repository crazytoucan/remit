{
  "name": "remit",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "build": "yarn build:commonjs && yarn build:esm",
    "build:commonjs": "tsc",
    "build:esm": "tsc -p tsconfig-esm.json"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.9.3"
  }
}
